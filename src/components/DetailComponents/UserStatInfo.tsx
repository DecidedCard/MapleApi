import { CheckStatType } from "@/types";

const UserStatInfo = ({
  characterStatInfo,
}: {
  characterStatInfo: CheckStatType;
}) => {
  return <div>UserStatInfo</div>;
};

export default UserStatInfo;
